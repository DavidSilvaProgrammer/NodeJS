const express = require('express'); // Importa o módulo 'express'
const app = express(); // Cria uma instância do Express

// Define uma rota para o caminho raiz ('/') que retorna uma mensagem de "Olá express" quando acessada pelo método GET
app.get('/', (req, res) => {
  res.send("Olá express");
});

// Inicia o servidor na porta 8080 e imprime uma mensagem no console quando estiver pronto
app.listen(8080, () => {
  console.log("Servidor escutando na porta 8080");
});

/**
Nesse código, estamos criando um servidor web usando o framework Express em Node.js. Definimos uma rota 
para o caminho raiz ('/'), que retorna uma mensagem simples de "Olá express" quando acessada pelo método GET. 
Em seguida, usamos o método listen() para iniciar o servidor na porta 8080 e imprimimos uma mensagem no 
console para indicar que o servidor está em execução.
**/

 servidor web usando o framework Express em Node.js.
